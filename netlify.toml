# Netlify builds the Expo web export (react-native-web) into web-build/.
[build]
  command = "npm run export:web"
  publish = "web-build"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
